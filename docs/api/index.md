# Sensor API Reference

This reference defines HTTP contracts used by sensors and sensor bridges.

All clients must send `Accept: application/json`,
`Content-Type: application/json`, and sensor bearer token.

<a class="md-button" href="openapi.yaml" download>Download OpenAPI YAML</a>

<swagger-ui src="openapi.yaml"/>
