# Entity Relationship Diagram

## Entities

### Organization (`App\Models\Organization`)

#### Attributes:

- `id` (integer)
- `name` (varchar)
- `slug` (varchar)
- `description` (text)
- `email` (varchar)
- `phone` (varchar)
- `website` (varchar)
- `logo` (varchar)
- `deleted_at` (datetime)
- `created_at` (datetime)
- `updated_at` (datetime)

### Area (`App\Models\Peoplecount\Area`)

#### Attributes:

- `id` (integer)
- `name` (varchar)
- `event_id` (integer)
- `deleted_at` (datetime)
- `created_at` (datetime)
- `updated_at` (datetime)

### AreaRecurringReset (`App\Models\Peoplecount\AreaRecurringReset`)

#### Attributes:

- `id` (integer)
- `area_id` (integer)
- `reset_value` (integer)
- `reset_time` (time)
- `timezone` (varchar)
- `notes` (text)
- `created_at` (datetime)
- `updated_at` (datetime)

### AreaSingleReset (`App\Models\Peoplecount\AreaSingleReset`)

#### Attributes:

- `id` (integer)
- `area_id` (integer)
- `reset_value` (integer)
- `effective_at` (datetime)
- `created_by` (integer)
- `notes` (text)
- `created_at` (datetime)
- `updated_at` (datetime)

### Assignment (`App\Models\Peoplecount\Assignment`)

#### Attributes:

- `id` (integer)
- `event_id` (integer)
- `area_id` (integer)
- `sensor_id` (integer)
- `direction_flipped` (tinyint)
- `active_from` (datetime)
- `active_to` (datetime)
- `deleted_at` (datetime)
- `created_at` (datetime)
- `updated_at` (datetime)

### Event (`App\Models\Peoplecount\Event`)

#### Attributes:

- `id` (integer)
- `name` (varchar)
- `organization_id` (integer)
- `starts_at` (datetime)
- `ends_at` (datetime)
- `deleted_at` (datetime)
- `created_at` (datetime)
- `updated_at` (datetime)

### IntervalCount (`App\Models\Peoplecount\IntervalCount`)

#### Attributes:

- `id` (integer)
- `sensor_id` (integer)
- `ts_from` (datetime)
- `ts_to` (datetime)
- `count_in` (integer)
- `count_out` (integer)

### Sensor (`App\Models\Peoplecount\Sensor`)

#### Attributes:

- `id` (integer)
- `vendor` (varchar)
- `model` (varchar)
- `serial` (varchar)
- `api_token` (varchar)
- `organization_id` (integer)
- `deleted_at` (datetime)
- `created_at` (datetime)
- `updated_at` (datetime)

### User (`App\Models\User`)

#### Attributes:

- `id` (integer)
- `name` (varchar)
- `email` (varchar)
- `phone` (varchar)
- `email_verified_at` (datetime)
- `password` (varchar)
- `remember_token` (varchar)
- `created_at` (datetime)
- `updated_at` (datetime)

## Relationships

### Organization Relationships

- **BelongsToMany** `users` to User (Local Key: ``, Foreign Key: ``)
- **HasMany** `sensors` to Sensor (Local Key: `id`, Foreign Key: `organization_id`)
- **HasMany** `events` to Event (Local Key: `id`, Foreign Key: `organization_id`)
- **HasManyThrough** `areas` to Area (Local Key: ``, Foreign Key: ``)
- **HasManyThrough** `assignments` to Assignment (Local Key: ``, Foreign Key: ``)

### Area Relationships

- **BelongsTo** `event` to Event (Local Key: `event_id`, Foreign Key: `id`)
- **HasMany** `assignments` to Assignment (Local Key: `id`, Foreign Key: `area_id`)
- **HasMany** `areaSingleResets` to AreaSingleReset (Local Key: `id`, Foreign Key: `area_id`)
- **HasMany** `singleResets` to AreaSingleReset (Local Key: `id`, Foreign Key: `area_id`)
- **HasMany** `areaRecurringResets` to AreaRecurringReset (Local Key: `id`, Foreign Key: `area_id`)
- **HasMany** `recurringResets` to AreaRecurringReset (Local Key: `id`, Foreign Key: `area_id`)

### AreaRecurringReset Relationships

- **BelongsTo** `area` to Area (Local Key: `area_id`, Foreign Key: `id`)

### AreaSingleReset Relationships

- **BelongsTo** `area` to Area (Local Key: `area_id`, Foreign Key: `id`)
- **BelongsTo** `createdBy` to User (Local Key: `created_by`, Foreign Key: `id`)

### Assignment Relationships

- **BelongsTo** `event` to Event (Local Key: `event_id`, Foreign Key: `id`)
- **BelongsTo** `area` to Area (Local Key: `area_id`, Foreign Key: `id`)
- **BelongsTo** `sensor` to Sensor (Local Key: `sensor_id`, Foreign Key: `id`)

### Event Relationships

- **BelongsTo** `organization` to Organization (Local Key: `organization_id`, Foreign Key: `id`)
- **HasMany** `areas` to Area (Local Key: `id`, Foreign Key: `event_id`)
- **HasMany** `assignments` to Assignment (Local Key: `id`, Foreign Key: `event_id`)

### IntervalCount Relationships

- **BelongsTo** `sensor` to Sensor (Local Key: `sensor_id`, Foreign Key: `id`)

### Sensor Relationships

- **BelongsTo** `organization` to Organization (Local Key: `organization_id`, Foreign Key: `id`)
- **HasMany** `intervalCounts` to IntervalCount (Local Key: `id`, Foreign Key: `sensor_id`)
- **HasMany** `assignments` to Assignment (Local Key: `id`, Foreign Key: `sensor_id`)

### User Relationships

- **BelongsToMany** `organizations` to Organization (Local Key: ``, Foreign Key: ``)
- **HasMany** `areaSingleResets` to AreaSingleReset (Local Key: `id`, Foreign Key: `created_by`)

